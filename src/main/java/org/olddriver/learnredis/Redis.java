package org.olddriver.learnredis;

/**
 *
 */
public class Redis {
    /**
     * 4k读写
     * db-engines
     *
     * redis value的数据类型
     * string，hash，list，set，sorted set
     * value存在类型，可以获取value中部分数据，不必获取全量数据后解析
     *
     * redis安装
     * i.对源文件进行编译，在src目录下生成可执行文件
     * ii.安装redis，将可执行文件加入指定目录，默认加入/usr/local/bin下
     *    配置环境变量
     * iii.执行utils下install_server.sh脚本，将redis配置为系统服务
     */
}
